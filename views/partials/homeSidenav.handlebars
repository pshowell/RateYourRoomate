<nav class="nav flex-column">
    <span class="text-center mb-3">
        <img class="rounded mx-auto img-fluid" src="{{this.userPicture}}" alt="{{this.userName}}">
    </span>
    <p>{{userName}}</p>
    <div class="nav flex-column nav-pills">
        <a id="feed" class="nav-link" href="/api/posts">Feed</a>
        <a id="find-roommate-friend" class="nav-link" href="roommates">Find a Roommate</a>
    </div>
</nav>
